require "spec_helper"

describe SpreeChargebee do
  it "has a version number" do
    expect(SpreeChargebee::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
